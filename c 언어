#include<stdio.h>   /*이문장은 전처리기 지시문(#<- 전처리기 지시자)*/ /*c는 함수로 구성*/
int main()          /*2~6번까지 함수정의 */          /*main, print 함수*/    /*{}<-함수의 몸체 */    /*2는 함수의 헤더*/
{
	printf("Hello, World!");
	return 0;
}
/*int<- 자료형 main 앞에는 int 만*/
/*함수뒤에는 괄호*/
/*int와 return은 같이 */
/*return은 함수의 종료를 의미*/
/*printf();  <-출력함수 호출문 c파생 관련 언어만 */
/*미리 정의된 함수는 c언어에서 라이브러리함수라불림 다른언어는 내장함수*//*c는 필요한것만 가져옴 c가 가벼운이유*/
/*함수는 정의후 호출*/
/*사용자가 정의한 함수를 사용자정의함수*/
/*studio.h가 정의되있는 함수를 쓸수있게함*/
/* studio.h는 input output 함수들이 있음*/
/* 프로그램 조건: 입력 자료 0개이상 출력 자료 1개이상*/
/* main 첫번쨰 메인 호출문c언어에 꼭필요*/
/*모든 함수는 동등*/
/*명령; =>명령문*/



